<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>paymentGW</title>
    <style>
        .flex {
			display: flex;
		}

		.item {
			flex: 1;
			padding: 2rem;
		}
	</style>
</head>

<body>
    <div class="flex">
        <div class="item">
            <h3>歡迎使用匯款方式付款</h3>
            <h4>此次訂單序號（會從後端傳過來）</h4>
            <% for ( var i = 0 ; i < o_IDs.length ; i++){ %>
                <input class="order" type="hidden" value=<%=o_IDs[i]%> >
                <h4><%=o_IDs[i]%></h4>
            <% } %>

            <input id="amount" type="hidden" value=<%=amount%> >
            <h4>此次付款金額（會從後端傳過來）</h4>
            <h4>
                <%=amount%>元</h4>
            <hr>
            <h4>請將這次付款金額匯入指定帳戶</h4>
            <% for ( var i = 0 ; i < v_account.length ; i++){ %>
                <input class="v_account" type="hidden" value=<%=v_account[i]%> >
                <h4><%=v_account[i]%></h4>
            <% } %>


        </div>
        <div class="item" style="width:500px;">
            <h3>活動紀錄</h3>
            <pre id="logger"></pre>
        </div>
    </div>

    <script src='http://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.0/jquery.min.js'></script>
    <script src="/javascripts/bank.js"></script>
</body>



</html>